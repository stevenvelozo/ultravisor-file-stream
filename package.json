{
	"name": "ultravisor-file-stream",
	"version": "0.0.1",
	"description": "Ultravisor file-streaming primitives: chunked write at offset with sha256-verified atomic finalize, buffered read, directory-tree hashing. Used by ultravisor task types and any beacon that needs to ship multi-GB files across the mesh.",
	"main": "source/Ultravisor-FileStream.cjs",
	"scripts": {
		"test": "npx mocha -u tdd --exit --timeout 10000 ./test/*_tests*.js"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/stevenvelozo/ultravisor-file-stream.git"
	},
	"keywords": [
		"ultravisor",
		"file-stream",
		"chunked-write",
		"sha256",
		"file-transfer"
	],
	"author": "Steven Velozo <steven@velozo.com> (http://velozo.com/)",
	"license": "MIT",
	"dependencies": {},
	"devDependencies": {
		"mocha": "^10.4.0"
	}
}
